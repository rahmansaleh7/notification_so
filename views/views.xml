<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="notification_so.list">
      <field name="name">notification_so list</field>
      <field name="model">notification_so.notification_so</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="notification_so.action_window">
      <field name="name">notification_so window</field>
      <field name="res_model">notification_so.notification_so</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="notification_so.action_server">
      <field name="name">notification_so server</field>
      <field name="model_id" ref="model_notification_so_notification_so"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="notification_so" id="notification_so.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="notification_so.menu_1" parent="notification_so.menu_root"/>
    <menuitem name="Menu 2" id="notification_so.menu_2" parent="notification_so.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="notification_so.menu_1_list" parent="notification_so.menu_1"
              action="notification_so.action_window"/>
    <menuitem name="Server to list" id="notification_so" parent="notification_so.menu_2"
              action="notification_so.action_server"/>
    -->
  </data>
</odoo>